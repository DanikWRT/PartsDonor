Read the file /home/aifactory/PartsDonor/_s1_spec.md fully — it is the authoritative S1 spec (PartsDonor R1 line «Доноры целиком»). Your job: COMPLETE the S1 frontend implementation that is currently half-finished and BROKEN in the working tree, then run the mandatory live verification from spec section F, write backend/_s1_verify.log, and capture the required screenshots.

CURRENT STATE (already done, do NOT redo): backend donor-lots API + tables + migration are COMPLETE and committed (GET /donor-lots, /donor-lots/{id}, /donor-lots/{id}/deals, /donor-lots/{id}/request, /donor-lots/{id}/requests all work live on http://127.0.0.1:8001; a seeded donor lot exists id=ba777dfc-7db7-43cd-8f6a-f707226f5a7d «Донор iPhone 13 Pro (комплект)» price 38000, seller СмартРемонт verified, component_count 5). Backend uvicorn is already running on 127.0.0.1:8001 and vite dev on 127.0.0.1:5173 — both healthy, do not restart unless needed.

FRONTEND IS BROKEN — fix it. cd /home/aifactory/PartsDonor/frontend && npm run build currently exits with error: in frontend/src/pages/DonorView.jsx the symbols STATUS_OPTIONS, DetailPanel, conditionText are DUPLICATED (defined near the top ~line 7 and again ~line 498) — a botched shared-component refactor left duplicate copies. The uncommitted working tree has: modified frontend/src/pages/DonorView.jsx, frontend/src/components/DonorExploded.jsx, frontend/src/pages/Catalog.jsx, frontend/src/App.jsx, frontend/src/styles.css, backend/_s1_seed.py; new untracked frontend/src/pages/DonorLot.jsx and DonorLots.jsx. Your fix: inspect these files, remove the duplicate block in DonorView.jsx (keep exactly ONE STATUS_OPTIONS / DetailPanel / conditionText), and make the shared-component refactor coherent — SLOT_META/statusCls/statusText/normalizeSlot/isFlat are now imported from components/DonorExploded.jsx so any local duplicate definitions of those must be fully removed too. Preserve all existing F7/UX-4 behavior and all other screens (no regressions). The Catalog.jsx donor toggle («Каталог деталей» | «Доноры целиком») and the DonorLot/DonorLots pages should keep working as written. Do not change files unrelated to fixing S1. npm run build must exit 0.

THEN run the full S1 live verification per spec section F with Playwright against http://127.0.0.1:5173: (a) catalog shows the toggle «Каталог деталей»|«Доноры целиком» and the «Доноры целиком» view shows the donor card with mini-exploded view + price + «5 деталей»; (b) /donor-lot/ba777dfc-7db7-43cd-8f6a-f707226f5a7d renders exploded view + price 38 000 ₽ + «Купить целиком» button; (c) login as a verified buyer (UX2 Demo Buyer — check backend seed / DB for a verified buyer with a buyer_profile; use /api/auth/login and store the token in localStorage pd-token and pd-session), click «Купить целиком» -> a Deal is created (GET /api/deals shows it), /donor-lots/{id} status becomes negotiated, and a whole-listing with donor_lot_id and status negotiated appears under /api/listings; record deal_id; (d) submit the «Оставить заявку» trade form (amount_rub + message) -> a donor_request is created with status pending; verify via GET /api/donor-lots/{id}/requests as seller or via SQL; (e) adaptive: at 400px and 1280px there is NO horizontal overflow (scrollWidth==clientWidth) and the donor views/buttons do not break. Note the whole-buy step uses 1-click deal logic requiring a VERIFIED company + filled BuyerProfile (billing_payer_name/inn/default_address) — seeded verified buyers already have these; if the seeded lot was already bought in a prior aborted run its status may be negotiated, so check /api/donor-lots first and if needed re-seed a fresh active donor lot via backend/_s1_seed.py or SQL (it is idempotent).

Write the full verify log to /home/aifactory/PartsDonor/backend/_s1_verify.log (each step, PASS/FAIL, facts/DOM metrics, deal_id). Save screenshots to /home/aifactory/PartsDonor: pd-s1-donor-catalog-desktop.png, pd-s1-donor-catalog-mobile.png, pd-s1-donor-lot-desktop.png, pd-s1-donor-lot-mobile.png, pd-s1-deal-desktop.png.

CONSTRAINTS: single-query mode blocks python3 -c and python3 -m pytest — for ad-hoc scripts write a .py file (e.g. with a heredoc or your file tool) and run it as python3 file.py. Do NOT git commit (the orchestrator commits per spec). Do NOT touch/restart the backend unless you must. Report: files changed, npm build exit code, all verification PASS/FAIL, deal_id, screenshot paths, path to backend/_s1_verify.log, and a short implementation summary. Read the spec for full design decisions (tables/routes/frontend are already in place — you only need to fix and verify).