ФИНАЛЬНАЯ ДОРАБОТКА РАЗВЁРТКИ (Perspective): объём у деталей уже есть (видны боковые грани/толщина и тени под каждой), НО перспективный наклон практически незаметен (±1.6° skew читается как «плоско, ортогонально»). Клиент хочет ощутимый 3D-разворот как на iFixit-подобных макетах.

РЕПО: /home/aifactory/PartsDonor. Меняй только frontend/src/pages/DonorView.jsx и frontend/src/styles.css. Backend/эндпоинты не трогай, UX (табы/легенда/клик-продано/список/чекбоксы/soldMap/заголовок) сохрани, реалистичные детали (блики/линзы/дорожки/контакты/штрих-код/текст Apple) не порти.

ЧТО СДЕЛАТЬ, чтобы перспектива стала НЕОСПОРИМО 3D:
1. Увеличь наклон слоёв до заметного: каждая деталь — внутренний <g> с transform вида `skewX(±10) rotate(±3–5)` (или `matrix`), знак меняется от глубины так, что вся колонка сходится к лёгкой перспективе (верхние и нижние слои наклонены в противоположные стороны относительно центра). Цель: детали читаются «в 3/4», а не плашмя. Не ломай размеры кнопки/клики — трансформируй только внутренний <g> детали.
2. Если от большого skew детали расползаются/обрезаются внутри кнопки — увеличь viewBox/высоту слоя (aspect-ratio) и подстрой позиции так, чтобы ничего не резало, деталь оставалась в границах кнопки и клик не ломался.
3. Добавь НЕЙТРАЛЬНЫЙ СТУДИЙНЫЙ ФОН-градиент контейнеру .pd-scheme (светло-серо-голубой радиальный, как фото-стол iFixit) вместо чистого чёрного — так тени/объём деталей будут контрастнее и читаться как объём, а не сливаться с чёрным.
4. Сохрани усиленные тени глубины под каждой деталью + ground shadow. При светлом фоне тени должны остаться заметными (не на чёрном).

КРИТЕРИЙ: рендер обязан читаться ОДНОЗНАЧНО как объёмный exploded view с перспективой (шов между ортогонально-плоским и «в 3/4» исчезает), при этом все 5 слоёв кликабельны, легенда на месте, статусы правильные.

ПРОВЕРКА:
  cd /home/aifactory/PartsDonor/frontend && npm run build  (без ошибок).
  Страница http://127.0.0.1:5173/donor/Apple/iPhone%2013%20Pro рендерится, 5 слоёв кликабельны, легенда на месте, статусы (board=продано красный, остальные зелёные/серый) на месте.

ОТЧИТАЙСЯ кратко: что изменил, что проверил. НЕ git commit.
