ДОРАБОТКА РАЗВЁРТКИ (Pi refinement pass): развёртка всё ещё читается «плоско», хотя уже есть градиенты/блики/тени. Нужно добавить НЕОСПОРИМЫЙ 3D-объём.

РЕПО: /home/aifactory/PartsDonor. Меняй только frontend/src/pages/DonorView.jsx и frontend/src/styles.css. Backend не трогай, эндпоинты не трогай, UX (табы/легенда/клик-продано/список/чекбоксы/soldMap) сохрани.

КОНТЕКСТ: сейчас слои — горизонтальные «плашки» (rect с градиентом + feDropShadow под каждым), лёгкий X-сдвиг колонки от глубины. Пользователь видит «плоские стикеры». Нужен настоящий псевдо-3D exploded-вид.

ЧТО СДЕЛАТЬ (конкретика):
1. У КАЖДОГО компонента добавь ВИДИМУЮ ТОЛЩИНУ (extrusion): под основным rect — «боковая стенка»/ребро той же формы (тёмный вертикальный параллелограмм/смещённый rect чуть ниже и вбок, цвет = затемнённый accent или чёрный с градиентом), как у физической детали с толщиной. Чтобы деталь выглядела как объёмный объект, а не наклейка. (Пример: корпус — заметная рамка-бока, аккумулятор — плотный чёрный брусок со скруглённой кромкой и светом сверху, плата — края-обрез PCB с просвечиванием слоёв.)
2. Добавь ЛЁГКИЙ ПЕРСПЕКТИВНЫЙ НАКЛОН НА КАЖДЫЙ СЛОЙ: оберни содержимое кнопки в SVG transform (например rotateX / skewX / небольшой rotate) чтобы каждая деталь смотрела чуть «в 3/4/профиль», а колонка слоёв — как реально разнесённая по глубине с перспективой. НЕ ломай координаты/размеры кнопки и клики — трансформируй только внутренний <g> детали.
3. Усиль тень глубины (feDropShadow) — больше blur + вертикальный offset под каждой деталью + лёгкая тень-подложка (ground shadow) внизу колонки.
4. Сохрани реалистичные детали (блики стекла, линзы камеры с отражениями, дорожки PCB, золотые контакты, штрих-код АКБ), просто добавь объём.

Критерий: рендер должен читаться как ОБЪЁМНЫЙ exploded view (толщина деталей, глубина колонки, перспектива), а не как плоские пластины.

ПРОВЕРКА:
  cd /home/aifactory/PartsDonor/frontend && npm run build  (без ошибок).
  Страница http://127.0.0.1:5173/donor/Apple/iPhone%2013%20Pro рендерится, 5 слоёв кликабельны, легенда на месте.

ОТЧИТАЙСЬ кратко: что добавил, что проверил. НЕ git commit.
