ЗАДАЧА FRONTEND-РАЗВЁРТКА PARTSDONOR: сделать exploded view реалистичным (объёмным), а не плоской схемой.

РЕПО: /home/aifactory/PartsDonor, ветка master. Frontend = Vite+React (порт 5173).
МЕНЯЙ ТОЛЬКО:
  frontend/src/pages/DonorView.jsx
  frontend/src/styles.css
(допустимо добавлять локальные asset-файлы в frontend/src/assets/, но только сгенерированные тобой SVG)

НЕ ТРОГАЙ backend, НЕ меняй эндпоинты, НЕ трогай логику табов/легенды/статусов/списка/чекбоксов. Меняется только ВИЗУАЛЬНОЕ представление развёртки («Разнесённый вид»).

ТЕКУЩАЯ ПРОБЛЕМА: слои развёртки — плоские стилизованные прямоугольники (DisplayLayer/BoardLayer/BatteryLayer/CameraLayer/BackcoverLayer c заливкой). Выглядит 2D-контуром, а нужен объёмный фото/псевдо-3D exploded view «как на макете пользователя», где компоненты выглядят как настоящие запчасти телефона (дисплей, плата, АКБ, камера, корпус), разнесённые по глубине с тенью под каждым слоем.

API /donor/{brand}/{model} (реальные данные, backend на 8001):
  { brand, model, exploded_view_url: <строка, СЕЙЧАС заглушка https://example.com/iphone13pro.png>,
    components:[{slot: display|board|battery|camera|backcover, title, price_rub, status: active|sold|negotiated|hidden, hotspot:{x:0..1,y:0..1}}] }
Начало страницы: GET http://127.0.0.1:8001/donor/Apple/iPhone%2013%20Pro

ЗАДАЧА:
1. Замени схематичные плоские SVG-слои на ПСЕВДО-3D РЕАЛИСТИЧНЫЕ компоненты с глубокой проработкой:
   - Реалистичные SVG-дерисовки запчастей (дисплей со стеклом/бликом/чёлкой/рамкой, материнская плата — зелёная PCB с золотыми контактами и чипом A15, аккумулятор — литий-полимерная плашка с маркировкой и контактом, камера — модуль с 3 линзами-стекляшками и вспышкой, задняя крышка/корпус с вырезом камеры и текстом Apple).
   - КАЖДЫЙ слой объёмный: многослойные градиенты (linearGradient/radialGradient), блики/света, внутренние тени, толщина кромки (bezel), отражение.
   - ПОД КАЖДЫМ слоем — мягкая тень (как в настоящем exploded-рендере: drop shadow с blur + offset по вертикали), чтобы читался 3D-объём и разнесение по глубине.
   - Лёгкая перспектива/наклон: вся колонка слоёв имеет чуть заметный изометрический/перспективный наклон (сдвиг по X в зависимости от z/depth), слои выше/ниже смещаются, добавляя глубину. Не ломай координаты кликов (кнопки остаются где были, просто рендер слоя внутри — с наклоном/градиентом).
2. Если exploded_view_url — реально достижимая картинка (не example.com, не пустая): показать её как фон-подложку развёртки и поверх наложить кликабельные точки. Если это заглушка/битая ссылка — просто рендерить псевдо-3D слои (основной путь). Сделай проверку через <img onError>/onLoad: рендерится как фон ТОЛЬКО если реально загрузилась.
3. Сохрани ВЕСЬ существующий UX:
   - Табы «Разнесённый вид / Список» (view state).
   - Легенда: зелёный=в наличии(active), красный=продано(sold), серый=прочее(foto/status: negotiated/hidden).
   - Клик по слою/точке = пометить «продано» (переключение soldMap).
   - Цвет точки/подписи каждого слоя соответствует статусу из backend (statusCls: sold→sold, active/""→in, иначе grey).
   - Чекбоксы в списке, синхронизация soldMap.
   - Заголовок «Развёртка: {model}».
   Компоненты ОБЯЗАНЫ оставаться кликабельными (button), точка+подпись поверх.

ОГРАНИЧЕНИЯ СТИЛЯ (styles.css) — уже есть классы, ПЕРЕИСПОЛЬЗУЙ:
  .pd-scheme,.pd-blowup,.pd-bg-svg,.pd-layer-btn,.pd-layer-svg,.pd-layer-tag,.pd-dot,.pd-legend,.pd-tabs,.pd-tab,.pd-part
  Можешь ДОБАВЛЯТЬ новые классы/правила, но не удаляй существующие нужные.
  Контейнер .pd-blowup: aspect-ratio 300/460, слои позиционируются в % (left/top, translate -50%,-50%), ширина слоя = (LYR_W/VIEW_W)*100 %.

КАК ПРОВЕРИТЬ:
  npm run build  (в frontend/) — без ошибок.
  curl http://127.0.0.1:8001/donor/Apple/iPhone%2013%20Pro — данные приходят.
  В браузере: http://127.0.0.1:5173/donor/Apple/iPhone%2013%20Pro — развёртка объёмная, слои кликабельны, легенда на месте.

СООБЩИ В ОТВЕТЕ: что изменил (кратко), что проверил (build/данные/рендер), путь к файлам. НЕ создавай git commit.
